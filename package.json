{
  "name": "remote",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bundle": "esbuild index.js --bundle --platform=node --format=cjs --outfile=dist/bundle.js --external:fsevents",
    "package": "npm run bundle && cp index.html dist/ && pkg dist/bundle.js --targets node18-win-x64 --output emsaz-remote.exe"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "obs-websocket-js": "^5.0.8",
    "socket.io": "^4.8.3",
    "ws": "^8.20.0"
  },
  "pkg": {
    "assets": [
      "dist/index.html"
    ],
    "outputPath": "."
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "pkg": "^5.8.1"
  }
}
